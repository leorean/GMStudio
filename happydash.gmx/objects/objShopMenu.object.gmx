<!--This Document is generated by GameMaker, if you edit it by hand then you do so at your own risk!-->
<object>
  <spriteName>&lt;undefined&gt;</spriteName>
  <solid>0</solid>
  <visible>-1</visible>
  <depth>0</depth>
  <persistent>0</persistent>
  <parentName>objAll</parentName>
  <maskName>&lt;undefined&gt;</maskName>
  <events>
    <event eventtype="0" enumb="0">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>depth = HUDDEPTH+2;
mask_index = sprParticleMask;

with(objPlayer)
    instance_destroy();
    
global.xSpeed = 0; //lol...
global.player = noone;

itemFocus = -1;
itemPrice = 0;
itemLocked = false;
itemName = "";

itemDescription = "";
descriptionTimer = 0;

////////////////////////////SHOP ITEMS///////////////////////////////

//createItem(x, y, index, name, description, pricearray, locked, tier);

//implemented
p = ds_list_create();
ds_list_add(p,1000); ds_list_add(p,2500);
item[UPGRADE.upHealth] = createItem(1*TILE, 2.5*TILE, UPGRADE.upHealth, "Extra Health", "Start the game with more health.", 
    arrayFromList(p), global.upgrade[UPGRADE.upHealth,UPGRADE.LOCKED], global.upgrade[UPGRADE.upHealth,UPGRADE.TIER]);

//implemented
p = ds_list_create();
ds_list_add(p,1000);
item[UPGRADE.upJump] = createItem(5*TILE, 4.5*TILE, UPGRADE.upJump, "Jump, jump, jump!", "Increase jumps in mid-air!", 
    arrayFromList(p), global.upgrade[UPGRADE.upJump,UPGRADE.LOCKED], global.upgrade[UPGRADE.upJump,UPGRADE.TIER]);

//implemented
p = ds_list_create();
ds_list_add(p,30); ds_list_add(p,80); ds_list_add(p,250);
item[UPGRADE.upMagnet] = createItem(9*TILE, 2.5*TILE, UPGRADE.upMagnet, "Coin Magnet", "Coins will be more attracted to you.", 
    arrayFromList(p), global.upgrade[UPGRADE.upMagnet,UPGRADE.LOCKED], global.upgrade[UPGRADE.upMagnet,UPGRADE.TIER]);

//implemented
p = ds_list_create();
ds_list_add(p,50); ds_list_add(p,100); ds_list_add(p,120); ds_list_add(p,350);
item[UPGRADE.upDash] = createItem(13*TILE, 4.5*TILE, UPGRADE.upDash, "Dashing Power", "Dash faster, further, longer!", 
    arrayFromList(p), global.upgrade[UPGRADE.upDash,UPGRADE.LOCKED], global.upgrade[UPGRADE.upDash,UPGRADE.TIER]);

//implemented
p = ds_list_create();
ds_list_add(p,100); ds_list_add(p,200); ds_list_add(p,350); ds_list_add(p,1000);
item[UPGRADE.upItemChance] = createItem(17*TILE, 2.5*TILE, UPGRADE.upItemChance, "Special Blocks", "Unlock special blocks and increase item chance.", 
    arrayFromList(p), global.upgrade[UPGRADE.upItemChance,UPGRADE.LOCKED], global.upgrade[UPGRADE.upItemChance,UPGRADE.TIER]);

//implemented
p = ds_list_create();
ds_list_add(p,200); ds_list_add(p,450); ds_list_add(p,650); ds_list_add(p,1000);
item[UPGRADE.upPowerUpTime] = createItem(1*TILE, 6.5*TILE, UPGRADE.upPowerUpTime, "Power UP!", "Find better Power-Up's and increase their duration.", 
    arrayFromList(p), global.upgrade[UPGRADE.upPowerUpTime,UPGRADE.LOCKED], global.upgrade[UPGRADE.upPowerUpTime,UPGRADE.TIER]);

//implemented
p = ds_list_create();
ds_list_add(p,100); ds_list_add(p,250); ds_list_add(p,400);
item[UPGRADE.upComboCooldown] = createItem(9*TILE, 6.5*TILE, UPGRADE.upComboCooldown, "Combo Cooldown", "Increase the combo cooldown duration.", 
    arrayFromList(p), global.upgrade[UPGRADE.upComboCooldown,UPGRADE.LOCKED], global.upgrade[UPGRADE.upComboCooldown,UPGRADE.TIER]);

p = ds_list_create();
ds_list_add(p,20000);
item[UPGRADE.upSuperDash] = createItem(17*TILE, 6.5*TILE, UPGRADE.upSuperDash, "SUPER DASH", "Unleash your full potential!", 
    arrayFromList(p), global.upgrade[UPGRADE.upSuperDash,UPGRADE.LOCKED], global.upgrade[UPGRADE.upSuperDash,UPGRADE.TIER]);

loadProgress();

////////////////////////////////////////////////////////////////////

////BUTTONS
index = 0;
maxIndex = 1;

//purchase
btn[0] = instance_create(WIDTH-2.5*TILE,HEIGHT-2.5*TILE,objButton);
btn[0].text = "";
btn[0].index = 0;
btn[0].parent = id;
btn[0].fnt = fntMenu;
btn[0].w = 2*TILE;
btn[0].h = 2*TILE;

//back to game
btn[1] = instance_create(WIDTH-TILE,0,objButton);
btn[1].text = "";
btn[1].index = 1;
btn[1].parent = id;
btn[1].w = 1*TILE;
btn[1].h = 2*TILE;

//exit to main menu
btn[2] = instance_create(TILE,0,objButton);
btn[2].text = "";
btn[2].index = 2;
btn[2].parent = id;
btn[2].w = 1*TILE;
btn[2].h = 2*TILE;

if (global.input != INPUT_TOUCH)
    global.mouseVisible = true;
</string>
          </argument>
        </arguments>
      </action>
    </event>
    <event eventtype="1" enumb="0">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>//instance_activate_object(objControl);
</string>
          </argument>
        </arguments>
      </action>
    </event>
    <event eventtype="3" enumb="0">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>if (room != SHOP) instance_destroy();

if (mouse_check_button_released(mb_left))
{
    i = instance_place(mouse_x,mouse_y,objShopItem);
    if (instance_exists(i))
    {
        itemFocus = i.index;
        descriptionTimer = 0;
    } else
        if (!btn[0].pressed) //only release focus when clicking elsewhere.
            itemFocus = -1;
    i = noone;  
}

if (itemFocus != -1)
{
    itemPrice = item[itemFocus].price[item[itemFocus].tier];
    if (!item[itemFocus].locked)
        suffix = " (Level " + string(item[itemFocus].tier) + " / " + string(item[itemFocus].maxTier) + ")";
    else
        suffix = " &lt;locked&gt;";
    itemName = string(item[itemFocus].name) + suffix;
    itemLocked = item[itemFocus].locked;
    if (!descriptionTimer)
        itemDescription = item[itemFocus].description;    
}
else
{
    itemPrice = "---";
    itemName = "";
    itemDescription = "";
}

descriptionTimer = max(descriptionTimer -1,0);

if (btn[0].pressed)
{
    if (itemFocus != -1)
    {
        if (item[itemFocus].tier &lt; item[itemFocus].maxTier)
        {
            if (itemLocked)
                itemDescription = "This item is currently unavailable.";
            else if (real(itemPrice) &lt;= global.wallet) //THIS IS WHERE THE MAGIC HAPPENS
            {
                var t;
                if (item[itemFocus].tier == 0) //unlocking effect
                {
                    t = instance_create(item[itemFocus].x,item[itemFocus].y,objEffectDust);t.depth=item[itemFocus].depth-1;
                    t = instance_create(item[itemFocus].x+TILE,item[itemFocus].y,objEffectDust);t.depth=item[itemFocus].depth-1;
                    t = instance_create(item[itemFocus].x,item[itemFocus].y+TILE,objEffectDust);t.depth=item[itemFocus].depth-1;
                    t = instance_create(item[itemFocus].x+TILE,item[itemFocus].y+TILE,objEffectDust);t.depth=item[itemFocus].depth-1;
                    t = instance_create(item[itemFocus].x+.5*TILE,item[itemFocus].y+.5*TILE,objEffectDust);t.depth=item[itemFocus].depth-1;
                }
                t = instance_create(WIDTH/2,TILE,objText);
                t.text = "-"+string(itemPrice);
                t.yVel = -1;
                t.yGrav = .1;
                t.fnt = fntScoreSmall;
                t.depth = MOUSEDEPTH+1;
                
                global.wallet -= real(itemPrice);
                item[itemFocus].tier = min(item[itemFocus].tier + 1, item[itemFocus].maxTier);
                
                global.upgrade[item[itemFocus].index,UPGRADE.TIER]+=1; //save global upgrade.
                
                itemDescription = "Thank you!";
            } else
                itemDescription = "You can't afford that. Earn more coins!";
        } else
            itemDescription = "Already at MAX level.";
        //TODO: SAVE THE PURCHASE
    }
    descriptionTimer = 1.5*FPS;
}

if (btn[1].pressed || keyboard_check_pressed(vk_space) || (itemFocus == -1 &amp;&amp; keyboard_check_pressed(vk_enter))) //PLAY
{
    saveProgress();
    loadProgress();

    instance_activate_object(objControl);
    room_goto(MAIN);
}
if (btn[2].pressed || keyboard_check_pressed(vk_escape))
{
    instance_activate_object(objControl);
    room_goto(INIT);
}

if (debug_mode)
{
    if (keyboard_check_pressed(ord('R')))
    {
        deleteProgress();
        //room_restart();
    }
    if (keyboard_check_pressed(ord('T')))
        global.wallet += 50;        
}
</string>
          </argument>
        </arguments>
      </action>
    </event>
    <event eventtype="8" enumb="0">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>draw_set_alpha(1);
//draw_set_color(c_maroon);
//draw_rectangle(0,0,WIDTH,HEIGHT,0);

draw_sprite_part(spriteShop,-1,0,0,WIDTH,HEIGHT,0,0);

//PURCHASE BUTTON
var b;
b = 0;
if (btn[0].active &amp;&amp; mouse_check_button(mb_left)) b = 1;
if (itemFocus != -1)
    if (itemLocked || real(itemPrice) &gt; global.wallet || item[itemFocus].tier &gt;= item[itemFocus].maxTier) b = 2;
draw_sprite_part(spriteShop,-1,4*TILE*b,HEIGHT,4*TILE,2*TILE,WIDTH-4.5*TILE,HEIGHT-2.5*TILE);

//EXIT BUTTON
draw_sprite_part(spriteShop,-1,2*TILE*(btn[2].active &amp;&amp; mouse_check_button(mb_left)),HEIGHT+2*TILE,2*TILE,2*TILE,0,0);

//PLAY BUTTON
draw_sprite_part(spriteShop,-1,2*TILE*(btn[1].active &amp;&amp; mouse_check_button(mb_left)),HEIGHT+4*TILE,2*TILE,2*TILE,WIDTH-2*TILE,0);

draw_set_color(c_white);
draw_set_font(fntMenu);
draw_set_halign(fa_center);
draw_set_valign(fa_top);

drawText(WIDTH/2,1,string(global.wallet),c_white,c_black,fntScoreSmall);

//CURRENT DESCRIPTION
draw_set_halign(fa_left);
drawText(TILE,HEIGHT-2.5*TILE,itemDescription,c_white,c_none,fntShopSmall);

//CURRENT PRICE
draw_set_halign(fa_right);
var c1,c2;
if (itemFocus != -1)
{
    var c1,c2;
    
    if (itemLocked)
        {c1 = c_gray; c2 = c_none}
    else if (real(itemPrice) &lt;= global.wallet)
        {c1 = c_white;c2 = c_black}
    else
        {c1 = c_red;c2 = c_none}
}
else {c1 = c_white; c2 = c_black};

drawText(WIDTH-.5*TILE,HEIGHT-4*TILE,string(itemPrice),c1,c2,fntShopSmall);


//ITEM NAME
draw_set_halign(fa_left);
drawText(.5*TILE,HEIGHT-4*TILE,string(itemName),make_color_hsv(33,170,255),c_none,fntShopSmall);
</string>
          </argument>
        </arguments>
      </action>
    </event>
  </events>
  <PhysicsObject>0</PhysicsObject>
  <PhysicsObjectSensor>0</PhysicsObjectSensor>
  <PhysicsObjectShape>0</PhysicsObjectShape>
  <PhysicsObjectDensity>0.5</PhysicsObjectDensity>
  <PhysicsObjectRestitution>0.100000001490116</PhysicsObjectRestitution>
  <PhysicsObjectGroup>0</PhysicsObjectGroup>
  <PhysicsObjectLinearDamping>0.100000001490116</PhysicsObjectLinearDamping>
  <PhysicsObjectAngularDamping>0.100000001490116</PhysicsObjectAngularDamping>
  <PhysicsObjectFriction>0.200000002980232</PhysicsObjectFriction>
  <PhysicsObjectAwake>-1</PhysicsObjectAwake>
  <PhysicsObjectKinematic>0</PhysicsObjectKinematic>
  <PhysicsShapePoints/>
</object>
